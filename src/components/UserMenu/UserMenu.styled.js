import styled from 'styled-components';

const LogOutBtn = styled.button.attrs({ type: 'button' })`
  padding: 6px 15px 6px 15px;
  cursor: pointer;
  margin-left: 15px;
`;
const UserBox = styled.div``;
const UserHeader = styled.span`
  font-size: 18px;
`;

export { LogOutBtn, UserBox, UserHeader };
