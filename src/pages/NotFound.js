const styles = {
  title: {
    fontWeight: 600,
    fontSize: 48,
    textAlign: 'center',
  },
};

const NotFound = () => {
  return <h1 style={styles.title}>Oops... Something went wrong. Try again.</h1>;
};
export default NotFound;
